# MultiAgentWorkspace 1.0 Dependencies
# Python 3.11+ required

# Windows process management (Job Object)
pywin32==311

# Testing framework
pytest==9.0.2
pytest-asyncio==1.3.0

# Optional compatibility helper
pywin32-ctypes==0.2.3

# Desktop UI framework
PySide6>=6.6.0

# Note: Claude CLI must be installed and available in PATH
